# Python codegens

For usage, create a [venv](https://docs.python.org/3/library/venv.html
) depending on your OS, activate it then `pip install Jinja2`.

Afterwards, on an activated env `python templates/<selected_template>.py` providing inputs given
at prompt.

